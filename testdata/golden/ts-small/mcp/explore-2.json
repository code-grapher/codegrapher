{
  "jsonrpc": "2.0",
  "id": 11,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "## Exploration: get set lookup\n\nFound 17 symbols across 4 files.\n\n### Blast radius \u2014 what depends on these (update/verify before editing)\n\n- `get` (src/store.ts:15) \u2014 1 caller in `src/cache.ts`; \u26a0\ufe0f no covering tests found\n- `set` (src/store.ts:21) \u2014 1 caller in `src/cache.ts`; \u26a0\ufe0f no covering tests found\n- `lookup` (src/cache.ts:8) \u2014 1 caller in `src/cache.ts`; \u26a0\ufe0f no covering tests found\n- `LookupResult` (src/store.ts:8) \u2014 1 caller in `src/store.ts`; \u26a0\ufe0f no covering tests found\n\n### Source Code\n\n> The code below is the **verbatim, current on-disk source** of these files \u2014 re-read from disk on this call and line-numbered, byte-for-byte identical to what the Read tool returns. It is NOT a summary, outline, or stale cache. Treat each block as a Read you have already performed: do not Read a file shown here.\n\n#### src/store.ts \u2014 get(method), set(method), LookupResult(type_alias), Store(class), items(method), +4 more\n\n```typescript\n1\t/** A key-value entry. */\n2\texport interface Entry {\n3\t  key: string;\n4\t  value: string;\n5\t}\n6\t\n7\t/** Result kind for store operations. */\n8\texport type LookupResult = Entry | undefined;\n9\t\n10\t/** In-memory key-value store. */\n11\texport class Store {\n12\t  private items = new Map<string, string>();\n13\t\n14\t  /** Get a value by key. */\n15\t  get(key: string): LookupResult {\n16\t    const value = this.items.get(key);\n17\t    return value === undefined ? undefined : { key, value };\n18\t  }\n19\t\n20\t  /** Store a value under a key. */\n21\t  set(key: string, value: string): void {\n22\t    this.items.set(key, normalize(value));\n23\t  }\n24\t\n25\t  get size(): number {\n26\t    return this.items.size;\n27\t  }\n28\t}\n29\t\n30\texport function normalize(value: string): string {\n31\t  return value === '' ? '<empty>' : value;\n32\t}\n33\t\n34\texport const describe = (entry: Entry): string => `${entry.key}=${entry.value}`;\n```\n\n#### src/cache.ts \u2014 lookup(method), Cache(class), report(method), warm(method), backend(method), +1 more\n\n```typescript\n1\timport { Store, type Entry, describe } from './store';\n2\t\n3\t/** Read-through cache with hit counting. */\n4\texport class Cache {\n5\t  private backend = new Store();\n6\t  private hits = 0;\n7\t\n8\t  lookup(key: string): Entry | undefined {\n9\t    const entry = this.backend.get(key);\n10\t    if (entry !== undefined) {\n11\t      this.hits += 1;\n12\t    }\n13\t    return entry;\n14\t  }\n15\t\n16\t  warm(pairs: Record<string, string>): void {\n17\t    for (const [key, value] of Object.entries(pairs)) {\n18\t      this.backend.set(key, value);\n19\t    }\n20\t  }\n21\t\n22\t  /** Render cache stats for logging. */\n23\t  async report(): Promise<string> {\n24\t    const greeting = this.lookup('greeting');\n25\t    const line = greeting ? describe(greeting) : 'cold';\n26\t    return `${line} (hits=${this.hits})`;\n27\t  }\n28\t}\n```\n\n#### src/widget.tsx \u2014 Widget(function)\n\n```tsx\n1\texport function Widget(props: { label: string; count: number }) {\n2\t  return (\n3\t    <div className=\"widget\">\n4\t      <span className=\"label\">{props.label}</span>\n5\t      <strong>{props.count}</strong>\n6\t    </div>\n7\t  );\n8\t}\n```\n"
      }
    ]
  }
}
