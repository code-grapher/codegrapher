```yaml
feature: whole-repo-file-nodes
revision: 07eec76
verdicts:
  - ac: whole-repo-file-nodes#ac:gitignored-file-excluded
    verdict: pass
    justification: "ScanDirectory returns only non-gitignored files; TestScanDirectoryGitignoredFileExcluded asserts tracked file admitted, gitignored file excluded entirely."
  - ac: whole-repo-file-nodes#ac:unknown-extension-gets-node
    verdict: pass
    justification: "TestUnknownExtensionGetsBareFileNode asserts exactly 1 node (KindFile, LangUnknown), 0 symbols; ExtractFile early-returns one bare file node for LangUnknown."
  - ac: whole-repo-file-nodes#ac:binary-file-gets-node
    verdict: pass
    justification: "TestBinaryFileGetsBareFileNode writes a .png exceeding MaxFileSize and asserts one KindFile/LangUnknown node; init.go bypasses the size cap for unknown-lang, no parse."
  - ac: whole-repo-file-nodes#ac:recognized-source-unchanged
    verdict: pass
    justification: "Additive LangUnknown early-return leaves the recognized path untouched; TestRecognizedSourceStillExtracted passes and go-small golden symbol nodes are unchanged."
  - ac: whole-repo-file-nodes#ac:gates-green-after-rebaseline
    verdict: pass
    justification: "gofmt clean, go vet exit 0, CGO_ENABLED=0 build exit 0, CGO_ENABLED=0 test exit 0 (incl. paritytest); 74 goldens regenerated via tools/parity scripts."
```

## AC: gitignored-file-excluded

**Verdict:** pass

**Justification:** `ScanDirectory` now returns only non-gitignored files (git-visible / `.gitignore`-filtered walk), so a gitignored file never enters the candidate list and no node of any kind is created for it. `TestScanDirectoryGitignoredFileExcluded` asserts a tracked `notes.txt` is admitted while a gitignored `secret.txt` is excluded entirely; the test passes.

**Commits:**
- 47e4e21 — feat(indexer): index every non-gitignored file as a file-level node

**Evidence:**
- 47e4e21 — `indexer/scan.go`: `ScanDirectory` returns `gitVisibleFiles` / `.gitignore`-filtered walk, no per-file source filter
- `indexer/scan_test.go` — `TestScanDirectoryGitignoredFileExcluded` (want `[.gitignore, notes.txt]`, `secret.txt` absent)
- `go test -run TestScanDirectoryGitignoredFileExcluded ./indexer/` → ok

## AC: unknown-extension-gets-node

**Verdict:** pass

**Justification:** `TestUnknownExtensionGetsBareFileNode` asserts exactly one node (`KindFile`, `LangUnknown`) for `notes.txt`; `ExtractFile` early-returns a single `emitFileNode` for `LangUnknown` with no symbols/edges/refs. Both the indexer and extract-level tests pass.

**Commits:**
- 47e4e21 — feat(indexer): index every non-gitignored file as a file-level node

**Evidence:**
- `indexer/unknown_file_test.go` — `TestUnknownExtensionGetsBareFileNode` (`len(nodes)==1`, `KindFile`, `LangUnknown`)
- `internal/extract/extract_unknown_test.go` — `TestUnknownLanguageEmitsBareFileNode` (1 node, 0 edges/refs/errors)
- `internal/extract/extract.go` — `LangUnknown` early-return: `emitFileNode(nil)`, no parse/symbols

## AC: binary-file-gets-node

**Verdict:** pass

**Justification:** `TestBinaryFileGetsBareFileNode` writes a `.png` exceeding `MaxFileSize` and asserts exactly one `KindFile`/`LangUnknown` node; `indexer/init.go` bypasses the size cap for unknown-language files and `ExtractFile(lang=unknown)` emits one bare file node before any tree-sitter parse. Test passes.

**Commits:**
- 47e4e21 — feat(indexer): index every non-gitignored file as a file-level node

**Evidence:**
- `indexer/unknown_file_test.go` — `TestBinaryFileGetsBareFileNode` (blob = PNG header + `MaxFileSize`+1024 bytes; asserts `len==1`, `KindFile`, `LangUnknown`)
- `indexer/init.go` — `extractOne`: `pathLang != LangUnknown && size > MaxFileSize` cap bypass; `lang == LangUnknown` returns bare node, no parse
- `internal/extract/extract.go` — `ExtractFile`: `lang == LangUnknown` emits single file node, returns before parse

## AC: recognized-source-unchanged

**Verdict:** pass

**Justification:** The production change is a strictly additive `LangUnknown` early-return branch in `extract.go`; the recognized-language parse/symbol path is untouched. `TestRecognizedSourceStillExtracted` asserts a recognized `main.go` still yields its `helper` symbol node, and the `go-small` golden's symbol nodes are byte-identical save for re-index `updatedAt` churn. Test passes.

**Commits:**
- 47e4e21 — feat(indexer): index every non-gitignored file as a file-level node

**Evidence:**
- `internal/extract/extract.go` — additive `if lang == model.LangUnknown` branch before the parse path
- `indexer/unknown_file_test.go` — `TestRecognizedSourceStillExtracted` (asserts `helper` symbol + >1 node for `main.go`)
- `testdata/golden/go-small/query.json` — symbol nodes unchanged (only `updatedAt` churn)

## AC: gates-green-after-rebaseline

**Verdict:** pass

**Justification:** Against the committed tree at HEAD (`07eec76`, includes `47e4e21`): `gofmt -l .` clean, `CGO_ENABLED=0 go vet ./...` exit 0, `CGO_ENABLED=0 go build ./...` exit 0, `CGO_ENABLED=0 go test -count=1 ./...` exit 0 (all packages ok, including `internal/paritytest` which validates output against the goldens). The commit regenerated 74 `testdata/golden/**` files via the `tools/parity` scripts.

**Commits:**
- 47e4e21 — feat(indexer): index every non-gitignored file as a file-level node

**Evidence:**
- `gofmt -l .` → no output (exit 0)
- `CGO_ENABLED=0 go vet ./...` → exit 0
- `CGO_ENABLED=0 go build ./...` → exit 0
- `CGO_ENABLED=0 go test -count=1 ./...` → exit 0 (all ok, incl. `internal/paritytest`)
- `git show 47e4e21 --stat` → 74 `testdata/golden/**` files regenerated
